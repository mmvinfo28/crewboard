{
  "name": "crewboard-core",
  "version": "0.1.0",
  "private": true,
  "description": "Crewboard multi-agent orchestration core (zero runtime dependencies).",
  "scripts": {
    "test": "node --test"
  }
}
